# Per-project pipeline configuration.
#
# Format: KEY=VALUE, one per line. Lines starting with # are ignored.
# See VARIABLES.md for what each key does.

# Flutter SDK version, installed and pinned by FVM.
FLUTTER_VERSION=3.44.6

# GitLab repository to build.
GIT_REPO_URL=https://gitlab.webelight.co.in/webelight/ccmt/chinmaya-mission-flutter.git

# Branch to clone.
GIT_BRANCH=main

# Jenkins credential holding the GitLab access token.
# Kind: Username with password  (username: oauth2, password: the token)
GIT_CREDENTIALS_ID=glpat-CvGTjPo90yv75S_CAfhf6286MQp1Ojg2CA.01.0y1ju8wvv

# Shorebird auth token, held in Jenkins Credentials as "Secret text".
SHOREBIRD_TOKEN_CREDENTIALS_ID=shorebird-token
